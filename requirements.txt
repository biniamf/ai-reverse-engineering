Flask==3.1.2
openai==2.8.1
Requests==2.32.5
# Production WSGI server used by the container image (webui/Dockerfile) and the
# documented production run. Not required for local `python webui/app.py`.
gunicorn==23.0.0
